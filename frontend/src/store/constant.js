import { GOOGLE_CREDENTIALS, SPREADSHEET_ID } from '@env';

export const googleCredentials = GOOGLE_CREDENTIALS;
export const spreadsheetId = SPREADSHEET_ID;

const constants = {
  googleCredentials,
  spreadsheetId,
};

export default constants;
